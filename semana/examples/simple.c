int main(set argc, int lopa) {
  int num;
  // float num;

  num = lopa + 2;

  if(exists(num in argc)) num = num * 3;
  if(!exists(num in argc)) {
    float result;

    add(num in argc);
    result = num - -1.75;
  }

  {
    float berada;
  }

  return 0;
}

int seta() {
  float beterraba;

  return 0;
}